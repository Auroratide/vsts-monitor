Build monitor for VSTS.  To activate:

**Install dependencies.**

```
npm install
```

**Set environment variables:**

* `VSTS_AUTHORIZATION`: Base64 encoded `username:personal_access_token`
* `PROJECT_ID`: Project UUID in VSTS

**Activate.**

```
npm start
```
